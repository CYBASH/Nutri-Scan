specVersion: "v1beta"
serviceId: "nutriscan"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nutriscan-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
